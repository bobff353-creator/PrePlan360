export default async function DemoPage({ searchParams }: { searchParams: Promise<{ owner?: string }> }) {
  const ownerMode = (await searchParams).owner === "1";
  return <main className="demo-shell"><header className="demo-header"><a className="demo-back" href="/" aria-label="Back to demo launch page"><span aria-hidden="true">←</span> Overview</a><div className="demo-title"><strong>PrePlan <span>360</span></strong><small>{ownerMode ? "Owner usable foundation demo" : "Latest view-only build"}</small></div><div className="demo-mode"><i/> Fictional data · {ownerMode ? "device-local editing enabled" : "editing locked"}</div></header><iframe className="fireflow-frame" src={ownerMode ? "/fireflow-360-demo.html?v=20260813-owner-daily-log-layout" : "/fireflow-360-demo.html?view=readonly&v=20260813-daily-log-view-only"} title={ownerMode ? "PrePlan 360 owner usable foundation demo" : "PrePlan 360 latest view-only demo"}/></main>;
}
